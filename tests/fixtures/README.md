# Test fixtures

Synthetic or tiny files here are **TEST FIXTURES ONLY**. They must not be
loaded as application/demo patient state.

- `tests/data/mini.vcf` — small real-format VCF used by ingest tests
- Inline TXT strings in `tests/test_clinical_pipeline.py`
